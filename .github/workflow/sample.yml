name: shell commands
on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo string 
        run: echo "Hello World"
      - name: display multiple commands
        run: |
          node -v 
          npm -v

